# AI_NOTES

## 1. Which parts of the code were AI-generated vs. written by me
- I wrote the models.py from the structure Claude proposed. 
- Both the function bodies in the storage.py were written by me. Claude gave me the function signatures and the comment scaffolding.
- In main.py, POST/GET and category filter generated by Claude Code from my written contract, and I wrote the summary and delete.
- The isolation fixtures in tests/conftest.py were generated by Claude.
- All the tests in the tests/test_expenses.py were written by me.

## 2. What I validated, tested, or changed in the AI's output and why
- The pip freeze without an activated venv produced 100+ unrelated packages at wrong versions caught by comparing against site-packages.If I had left it, the reviewer would have installed.
- PowerShell wrote requirements.txt as UTF-16; git flagged it as binary; rewrote it by hand as UTF-8. The UTF-16 encoding would have made the pip install fail with parsing errors for every line, may fail to parse the file.
- Added "ensure_ascii=False" to the storage layer, then verified with an accented category that the file stays readable. This will prevent café from turning into caf\u00e9 in the JSON.
- Rounded per-category values after noticing only the total was rounded. Because otherwise 250.50 + 120.30 would have returned 370.7999999995.
- I broke "category.lower()" to observe error handling and it worked as expected.
- My first delete had inverted logic and a misplaced paren making the condition always true - caught in review before running.
- Claude Code deleted my working data file while implementing an unrelated endpoint.

## 3. Any AI suggestion I decided not to use, and why
- The "#pyrefly: ignore [missing-import]" suppression - the actual problem was a misconfigured interpreter, not a missing package.
- The httpx2 deprecation warning - chose not to chase it. 